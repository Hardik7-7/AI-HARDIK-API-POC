Login to http://fresh-test.corp.coriolis.in/ using username "colama" and password "coriolis" and then list all servers and mark any random server in maintenance state , then list library and select any random library to deploy on that server. Make sure the deployment fails and we see the correct failure message. After that, unmark the server from maintenance state. Generate only 1 testcase.